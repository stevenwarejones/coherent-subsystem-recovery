# Optional. Only for the second, independent verification route
# proofs/verify_alternate.py, which re-checks the same certificate with a
# different factorization and exact Sylvester determinants over python-flint.
# It is deliberately NOT required for the central theorem; do not make any
# solver or heavy dependency mandatory for the core exact check.
python-flint==0.9.0
